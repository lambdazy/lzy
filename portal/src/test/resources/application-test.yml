portal:
  portal-id: "portal"
  portal-api-port: "${random.port}"
  host: "localhost"
  iam-token: "token_${portal.portal-id}"
  stdout-channel-id: "${portal.portal-id}:stdout"
  stderr-channel-id: "${portal.portal-id}:stderr"
  slots-api-port: "${random.port}"
  channel-manager-address: "localhost:${random.port}"
  iam-address: "localhost:8443"
  allocator-address: "localhost:${random.port}"
  whiteboard-address: "${portal.allocator-address}"
