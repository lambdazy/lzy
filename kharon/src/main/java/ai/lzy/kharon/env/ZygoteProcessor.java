package ai.lzy.kharon.env;

import ai.lzy.kharon.env.manager.CachedEnvManager;
import ai.lzy.priv.v2.Operations;

public class ZygoteProcessor {

    CachedEnvManager envManager;

    void determineZygote(Operations.Zygote zygote) {

        /*envManager.saveEnvConfig(zygote.getEnv().)
        if (zygote.getName().equals("env-preparing")) {
            Operations.Zygote envZygote = Operations.Zygote.newBuilder()
                .setEnv(z).build()
        }*/
    }

}
