[OUTPUT]
    Name            yc-logging
    Match           *
    resource_id     {kubernetes/container_name}
    message_key     msg
    level_key       level
    folder_id       ${folder_id}
    authorization   iam-key-file:/etc/secret/sa-key.json