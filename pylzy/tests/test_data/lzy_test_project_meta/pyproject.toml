[project]
name = "lzy_test_project_meta"
version = "3.0.0"
requires-python = ">=3.8"
license = {file = "LICENSE.txt"}

dependencies = [
    "pylzy",
    "lzy_test_project"
]

[build-system]
requires = ["setuptools>=43.0.0", "wheel"]
build-backend = "setuptools.build_meta"
